# StrategyLab Improvement Plan

## 목적
- `StrategyLab`을 단순 전략 실험 UI가 아니라, 기준이 고정된 전략 연구 엔진으로 재정의한다.
- 베이스 전략과 알파 전략을 같은 축 위에서 비교할 수 있도록 저장 규칙, 평가 규칙, UI 방향을 고정한다.
- 이후 구현 변경이 흔들리지 않도록 운영 원칙을 먼저 문서화한다.

## 1단계 목표
- 고정된 키움 조건식으로 포착되는 종목만 대상
- 고정된 베이스 전략만 사용
- 고정된 목표수익률만 사용
- 세금, 수수료, 슬리피지 차감 후 순수익 기준으로 평가
- 목표수익 달성 후 추가매매 금지
- 1회 매매 원칙 유지

### 1단계에서 1회 매매를 쓰는 이유
- 1단계의 목적은 최종 운영전략 확정이 아니라 기준선 확립이다.
- 첫 진입 신호의 품질을 다른 변수와 분리해서 보기 위해 1회 매매로 제한한다.
- 추가 진입, 재진입, 누적 매매 효과까지 동시에 섞이면 베이스 전략의 순수 진입 품질을 판단하기 어렵다.

### 1회 매매 규칙의 한계
- 우연히 첫 신호가 좋았던 사례가 성과를 왜곡할 수 있다.
- 다회 매매 환경에서의 대표성을 충분히 설명하지 못할 수 있다.
- 따라서 1회 매매 결과만으로 전략을 최종 판단하지 않는다.

## 1단계에서 고정할 변수

### 종목 소스
- 키움 조건식 이름 또는 ID 고정
- 조건 포착 시점 규칙 고정
- 테스트 대상 종목군은 같은 조건식에서만 선택

### 베이스 전략
- 표준 지표와 표준 타임프레임만 사용
- 충분히 최적화된 뒤 확정
- 확정 후에는 수정하지 않고 불변 버전으로 저장

### 목표수익률
- 초기에는 보수적 순수익 기준
- 예시 범위: `0.5%`, `1.0%`, `2.0%`
- 한 실험 세트에서는 목표수익률을 단일 값으로 고정

### 거래 규율
- 목표수익 달성 즉시 청산
- 추가매매 금지
- 재진입 금지
- 세션 종료 시 정리 규칙 고정

### 비용 모델
- 매수 수수료
- 매도 수수료
- 세금
- 슬리피지
- 위 항목을 모두 고정 수치로 반영

## 베이스 전략 원칙

### 왜 베이스 전략을 먼저 고정해야 하는가
- 기준이 흔들리면 알파가 실제 개선인지 판별할 수 없다.
- 약한 베이스 위의 개선은 진짜 알파가 아니라 베이스 결함 보정일 수 있다.
- 충분히 단련된 베이스가 있어야 알파의 구조적 기여를 측정할 수 있다.

### 베이스 전략 요구사항
- 표준 지표만 사용
- 표준 타임프레임만 사용
- 설명 가능한 규칙으로 구성
- 과도한 파라미터 최적화는 지양하되, 표준 범위 안에서는 충분히 최적화
- 단일 구간 최고 성능보다 다구간 안정성을 우선

### 베이스 전략 검증 기준
- 비용 차감 후 순수익 유지
- 목표수익 달성률 유지
- 종목 변경 시 과도하게 붕괴하지 않음
- 기간 변경 시 과도하게 붕괴하지 않음
- 파라미터 민감도가 과도하지 않음
- 규칙 단순화 후에도 완전히 붕괴하지 않음

## 버전 관리 원칙

### 불변화 규칙
- 한 번 확정된 베이스 전략은 절대 수정하지 않는다.
- 수정이 필요하면 기존 버전을 복제해 새 버전을 만든다.
- 저장된 버전은 모두 영구 보존한다.

### 파생 버전 규칙
- 지표 추가
- 조건 추가
- 조건 제거
- 목표수익률 변경
- 회피 규칙 추가
- 위와 같은 변경은 모두 새 버전으로 저장한다.

### 버전 간 관계
- 모든 버전은 부모 버전을 가진다.
- 기준 베이스에서 출발한 파생 계보를 추적할 수 있어야 한다.
- 각 버전은 변경 요약을 남긴다.

## 버전 선택 기준

### 단순화 원칙
- 선택 기준은 복잡하게 늘리지 않는다.
- 현재 버전 선택 핵심 항목은 `평균 수익률`로 둔다.

### 현재 운영 기준
- 버전 비교 화면에 `평균 수익률`을 항상 표시
- 추천 후보 판단 시 `평균 수익률`을 핵심 참고값으로 사용
- 이후 필요하면 보조 지표를 더하되, 기본 선택 기준은 단순 유지

## 알파 전략 원칙

### 알파 전략의 역할
- 베이스 전략을 대체하는 것이 아니라, 베이스 전략의 구조적 한계를 개선하는 것
- 지표 조합 미세조정보다 구조적 개선을 우선
- 개별 구간 과최적화보다 일반화 가능성을 우선

### 알파 엔진 투입 기준
- 충분히 검증된 베이스 버전 위에서만 수행
- 베이스 대비 무엇이 추가되었는지 명확해야 함
- 베이스 대비 어떤 약점을 보완하는지 명시해야 함
- 가장 우수한 베이스 버전을 알파 입력 기준으로 사용

## 단계별 검증 체계

### 1단계: 단일 진입 품질 검증
- 1회 매매만 허용
- 첫 신호의 품질을 평가
- 기준선 역할의 베이스 전략을 확립

### 2단계: 다회 매매 확장 검증
- 동일 조건식
- 동일 베이스 전략 또는 파생 버전
- 동일 목표수익률 체계 유지
- 추가 진입, 재진입, 누적 수익 구조를 검증

### 3단계: 일반화 검증
- 타종목 확대
- 다른 기간 적용
- 다양한 시장 상태에서 유지력 확인

## 데이터 모델 개선 방향

### 베이스/버전 저장에 필요한 항목
- `StrategyLineId`
- `StrategyVersionId`
- `ParentVersionId`
- `VersionTag`
- `VersionType`
  - `Base`
  - `Derived`
  - `AlphaCandidate`
- `SourcePrompt`
- `ChangeSummary`
- `AverageReturnRate`
- `SavedAt`
- `ValidationSnapshot`
- `ImmutableHash`

### 평가 리포트에서 유지할 핵심 항목
- `AverageReturnRate`
- `PrimaryMetric`
- `SecondaryMetric`
- `MaxDrawdownRate`
- `WinRate`
- `SampleCount`

## UI 개선 방향

### 메인 화면
- 프롬프트 입력
- 현재 전략 요약
- 평균 수익률
- 베이스 대비 개선 요약
- 차트 위 신호 비교
- 다음 액션 버튼

### 상세 화면 또는 탭
- KPI grid
- baseline comparison
- diagnosis
- suggestions
- trades
- 버전 이력
- export/package 정보

### UI 원칙
- 메인 화면은 판단 중심
- 상세 수치와 로그는 탭으로 분리
- 사용자는 메인 화면에서 `평가 → 보완 → 저장 → 비교` 흐름만 따라가면 됨

## 구현 우선순위

### 1순위
- 베이스 전략 불변화 규칙 구현
- 버전 저장 모델 구현
- 부모-자식 계보 저장
- 평균 수익률 기반 버전 비교 정리

### 2순위
- `StrategyLabApp` 메인 화면을 프롬프트 중심으로 재구성
- 차트에 베이스 대비 개선 신호를 직접 표시
- 요약 중심 메인 화면과 상세 탭 분리

### 3순위
- 베이스 버전과 파생 버전 비교 기능 강화
- 버전별 성과 추세 확인
- 가장 우수한 베이스 버전 선택 기능 추가

### 4순위
- 알파 전략 생성 흐름 추가
- 베이스 대비 보완 이유와 구조적 차이 설명 강화
- 종목 확대, 기간 확대 검증 흐름 추가

## 즉시 다음 작업
1. `StrategyCore` 모델에 버전 개념 추가
2. `StrategyLabApp` 세션/후보 저장 구조를 버전 저장 구조로 확장
3. 평균 수익률 중심 버전 비교 UI 정리
4. 메인 화면을 프롬프트 중심 레이아웃으로 재설계
